name: '@prismoid/core - Release'

on:
  workflow_run:
    workflows:
      - '@prismoid/core - Test'
    branches:
      - 'main'
    types:
      - completed

jobs:
  release:
    name: Release @prismoid/core
    uses: loopholelabs/prismoid/.github/workflows/release.yml@main
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    with:
      package-directory: packages/core
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      npm-token: ${{ secrets.NPM_TOKEN }}
