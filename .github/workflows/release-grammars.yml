name: '@prismoid/grammars - Release'

on:
  workflow_run:
    workflows:
      - '@prismoid/grammars - Test'
    branches:
      - 'main'
    types:
      - completed

jobs:
  release:
    name: Release @prismoid/grammars
    uses: loopholelabs/prismoid/.github/workflows/release.yml@main
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    with:
      package-directory: packages/grammars
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      npm-token: ${{ secrets.NPM_TOKEN }}
