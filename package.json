{
  "private": true,
  "name": "prismoid",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "build:core": "yarn workspace @prismoid/core build",
    "build:grammars": "yarn workspace @prismoid/grammars build",
    "build:react": "yarn workspace @prismoid/react build",
    "build": "yarn build:core && yarn build:grammars && yarn build:react",
    "test:core": "yarn workspace @prismoid/core test",
    "test:grammars": "yarn workspace @prismoid/grammars test",
    "test:react": "yarn workspace @prismoid/react test",
    "test": "yarn test:core && yarn test:grammars && yarn test:react"
  },
  "workspaces": [
    "packages/core",
    "packages/grammars",
    "packages/react"
  ],
  "packageManager": "yarn@3.1.0"
}
